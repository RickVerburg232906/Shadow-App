{
  "name": "new-ui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gen:firebase-config": "node scripts/gen-firebase-config.js",
    "dev": "npm run gen:firebase-config && vite --host --port 3000 --strictPort",
    "build": "npm run gen:firebase-config && vite build && node scripts/copy-static.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^9.22.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}